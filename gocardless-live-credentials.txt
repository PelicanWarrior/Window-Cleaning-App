# GoCardless LIVE credentials template (keep real values in Supabase Edge Function secrets only)
# Set these in Supabase secrets when switching to live

GOCARDLESS_CLIENT_ID=your_live_client_id
GOCARDLESS_CLIENT_SECRET=your_live_client_secret
GOCARDLESS_WEBHOOK_ENDPOINT_SECRET=your_live_webhook_endpoint_secret
GOCARDLESS_ENV=live
GOCARDLESS_STATE_SECRET=your_random_state_secret

# Redirect URI to register in your live GoCardless app:
# https://<your-project-ref>.supabase.co/functions/v1/gocardless_oauth_callback

# Webhook endpoint URL for live GoCardless:
# https://<your-project-ref>.supabase.co/functions/v1/gocardless_webhook

# Set these as Supabase Edge Function secrets (do not store real values in git):
# supabase secrets set GOCARDLESS_ENV=live
# supabase secrets set GOCARDLESS_CLIENT_ID=<value>
# supabase secrets set GOCARDLESS_CLIENT_SECRET=<value>
# supabase secrets set GOCARDLESS_WEBHOOK_ENDPOINT_SECRET=<value>
# supabase secrets set GOCARDLESS_STATE_SECRET=<value>
